# Contributing

This release candidate is currently proprietary and is not accepting unsolicited
code contributions.

For internal development:
1. create a feature branch;
2. install `.[dev]`;
3. run `pytest`;
4. run `ruff check src tests`;
5. do not modify locked validation Gold files after a machine run.
